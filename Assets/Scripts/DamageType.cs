using UnityEngine;

namespace TD.Combat
{
  /// <summary>
  /// Ungrouped damage types used across the project.  Do not reorder without updating the explicit values.
  /// </summary>
  public enum DamageType
  {
    Physical  = 0,
    Pierce    = 1,
    Impact    = 2,
    Slash     = 3,
    Explosive = 4,
    Fire      = 5,
    Cold      = 6,
    Shock     = 7,
    Poison    = 8,
    Acid      = 9,
    Energy    = 10,
    Water     = 11
  }
}
